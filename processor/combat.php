<?php

/**
 * Combat processor - handles combat events.
 * Tracks combat start/end, knockouts/recoveries, deaths.
 * These events enrich NPC context and can trigger reactions.
 */

storeEvent($eventType, $timestamp, $gamets, $eventData);

echo "ok";
