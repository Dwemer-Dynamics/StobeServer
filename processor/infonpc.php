<?php

/**
 * Infonpc processor - Herika-compatible nearby-NPC context event.
 * Stored as context-only telemetry for prompt history/debugging.
 */

storeEvent($eventType, $timestamp, $gamets, $eventData);

echo "ok";
